<style>
  /* Fake Purchase Popup Styles */
  .popup {
      position: fixed;
      bottom: 20px;
      left: 20px;
      background: #fff;
      padding: 10px;
      border-radius: 8px;
      box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
      display: flex;
      font-size: 14px;
      z-index: 9999;
      display: none;
      animation: fadeIn 0.5s ease-in-out;
      max-width: 430px;
  }

  .popup-content {
      display: flex;
      align-items: center;
      text-decoration: none;
      color: #333;
      width: 100%;
  }

  .popup-image {
      width: 80px;
      height: 80px;
      flex-shrink: 0;
      border-radius: 5px;
      overflow: hidden;
  }

  .popup-image img {
      width: 100%;
      height: 100%;
      object-fit: cover;
  }

  .popup-info {
      margin-left: 10px;
  }

  .popup-info p {
      margin: 2px 0;
  }

  .popup-info small {
      color: gray;
  }

  @keyframes fadeIn {
      from { opacity: 0; transform: translateY(10px); }
      to { opacity: 1; transform: translateY(0); }
  }
</style>

<!-- Fake Purchase Popup -->
<div id="fake-purchase-popup" class="popup">
  <a id="popup-link" href="#" target="_blank" class="popup-content">
    <div class="popup-image">
      <img id="popup-image" src="" alt="Product Image">
    </div>
    <div class="popup-info">
      <p><strong id="popup-name"></strong> from <span id="popup-location"></span></p>
      <p>purchased <strong id="popup-product"></strong></p>
      <p>
        <small>by <span id="popup-vendor"></span> • <span id="popup-time"></span></small>
      </p>
    </div>
  </a>
</div>
